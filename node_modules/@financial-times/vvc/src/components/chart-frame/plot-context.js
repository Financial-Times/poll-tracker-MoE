/**
 * @file
 * The plot Context object
 */
import { createContext } from 'react';
import { frameDefaultProps } from '../default-props';

// eslint-disable-next-line import/prefer-default-export
export const PlotContext = createContext({
  frameWidth: frameDefaultProps.width,
  frameHeight: frameDefaultProps.height,
  width: frameDefaultProps.width,
  height: frameDefaultProps.height,
  margin: frameDefaultProps.margin,
  padding: { top: 0, right: 50, bottom: 0, left: 20 },
  type: frameDefaultProps.type,
  updatePadding: () => {},
});
