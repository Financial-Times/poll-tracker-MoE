/**
 * @file
 * Library main entry point
 */

import * as components from './components';
import * as templates from './templates';
import * as utils from './util';
import * as parseData from './util/parseData';
import { approximateTextSize } from './util/victory-textsize';
import DebugComponent from './util/debug';

const util = {
  ...utils,
  parseData,
  approximateTextSize,
  DebugComponent,
};

export { components, templates, util };
