import { Meta, Story, Preview } from '@storybook/addon-docs/blocks';
import ChartFrame from '../../components/chart-frame';
import WaffleChart from './';
import EventWrapper from '../../util/event-wrapper';
import emptyData from '../../sample-data/ge2019-waffle-empty.json';
import partialData from '../../sample-data/ge2019-waffle-partial.json';
import fullData from '../../sample-data/ge2019-waffle-full.json';

<Meta title="Templates/Elections/Waffle" component={WaffleChart} />

# Waffle chart

Type: @todo
@todo

**When to use**

- @todo

@todo

**Examples from the FT:**

- @todo

**Questions?**

- Shepherd: TKTK
- Developer: TKTK

---

## Demos

---

### GE2019 Waffle Chart (empty, pre-results)

<Preview>
  <Story name="GE2019 Waffle Chart (empty)">
    <EventWrapper>
      {(cb) => (
        <WaffleChart
          data={emptyData}
          eventCallback={cb}
          filterKeys={['Conservative', 'Labour', 'Liberal Democrats', 'Scottish National Party']}
        />
      )}
    </EventWrapper>
  </Story>
</Preview>

### GE2019 Waffle Chart (mid-election)

<Preview>
  <Story name="GE2019 Waffle Chart (partial)">
    <EventWrapper>
      {(cb) => (
        <WaffleChart
          data={partialData}
          eventCallback={cb}
          filterKeys={['Conservative', 'Labour', 'Liberal Democrats', 'Scottish National Party']}
        />
      )}
    </EventWrapper>
  </Story>
</Preview>

### GE2019 Waffle Chart (final)

<Preview>
  <Story name="GE2019 Waffle Chart (complete)">
    <EventWrapper>
      {(cb) => (
        <WaffleChart
          data={fullData}
          eventCallback={cb}
          filterKeys={['Conservative', 'Labour', 'Liberal Democrats', 'Scottish National Party']}
        />
      )}
    </EventWrapper>
  </Story>
</Preview>

### GE2019 Waffle Chart (at minimum width)

<Preview>
  <Story name="GE2019 Waffle Chart (complete, smallest width needed)">
    <EventWrapper>
      {(cb) => (
        <WaffleChart
          title="How big was the swing between parties?"
          subtitle="Each square represents a constituency, arranged by the winning party’s margin of victory in 2017,|and coloured by the winner in 2019."
          stabRule={false}
          source={false}
          background={false}
          copyright={false}
          width="779"
          height="600"
          data={fullData}
          eventCallback={cb}
          filterKeys={['Conservative', 'Labour', 'Liberal Democrats', 'Scottish National Party']}
        />
      )}
    </EventWrapper>
  </Story>
</Preview>
