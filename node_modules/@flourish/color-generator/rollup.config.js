import nodeResolve from "rollup-plugin-node-resolve";

export default {
	input: "src/index.js",
	output: {
		dest: "color-generator.js",
		format: "umd",
		name: "colorgenerator"
	},
	plugins: [
		nodeResolve()
	]
};
